'use strict'

const PageInfo = `
  type PageInfo {
    totalCount: Int
    hasNextPage: Boolean
    hasPreviousPage: Boolean
    pageSize: Int
    pageNumber: Int
  }
`

module.exports = [
  PageInfo
]
